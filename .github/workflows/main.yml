name: Run social medial and mental health

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r Requirements.txt
        python -m nltk.downloader stopwords punkt

    - name: Run main script
      run: |
 HEAD
        python main.py

        python  main.py
 854f626e740a238dd98f9c72c2a085943965869b
